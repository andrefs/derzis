#!/bin/bash

# create named pipe if it doesn't exist
if [[ ! -f live.log ]]; then
  mkfifo worker.live.log
fi

zstd --adapt --flush-interval=1 <worker.live.log >>worker.log.zst &

unbuffer npm run dev 2>&1 | tee worker.live.log
