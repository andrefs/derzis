#!/bin/bash


SCRIPTPATH="$( cd -- "$(dirname "$0")" >/dev/null 2>&1 ; pwd -P )"

trap 'kill 0' EXIT

node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js &
node "$SCRIPTPATH"/worker.js

#bash
