#!/bin/bash

DBURI=$(node bin/getConfigVal.js db.uri)
echo Clearing $DBURI

mongo $DBURI --eval "db.domains.drop()"
mongo $DBURI --eval "db.paths.drop()"
mongo $DBURI --eval "db.resources.drop()"
mongo $DBURI --eval "db.triples.drop()"
