#!/bin/bash

DBNAME=$(node bin/getConfigVal.js db.name)

mongo $DBNAME --eval "db.domains.drop()"
mongo $DBNAME --eval "db.paths.drop()"
mongo $DBNAME --eval "db.resources.drop()"
mongo $DBNAME --eval "db.triples.drop()"
