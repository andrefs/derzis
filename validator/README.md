# Validator for derzis-crawler

A validator for Derzis crawling restrictions.

## Run

### Docker

#### Dependencies

* docker
* docker-compose

#### Running

* Run `docker-compose -f docker-compose.yml -f docker-validator.yml up --build`


